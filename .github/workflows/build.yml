name: Build Project

on:
  push:
    paths-ignore:
      - '.github/**'
  workflow_dispatch:

jobs:
  issues:
    uses: evanchooly/workflows/.github/workflows/build.yml@master
    secrets:
      GH_OAUTH: ${{ secrets.TESTING_GH_OAUTH }}
